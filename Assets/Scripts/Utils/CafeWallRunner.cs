using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CafeWallRunner : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        CafeWall.Main(new string[0]); // Just run the "Main" of the given script
    }
}
